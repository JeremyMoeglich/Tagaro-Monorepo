export const expire_time_weeks = 3;
export const expire_time_days = expire_time_weeks * 7;
export const expire_time_hours = expire_time_days * 24;
export const expire_time_minutes = expire_time_hours * 60;
export const expire_time_seconds = expire_time_minutes * 60;

export const expire_time_months = expire_time_weeks / 4;
